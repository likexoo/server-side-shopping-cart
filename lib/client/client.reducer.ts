import { CartItem } from "../type";
import { SCCActions } from "./client.action";

export const ShoppingCartClientReducer = (state: Array<CartItem> = [], action: SCCActions) => {
    // switch (action.type) {
    //     // case 'UPDATE_CART_ITEMS_ACTION': {
    //     //     break;
    //     // }
    // }
};
